# /probe-quick

Run the browser-probe skill in smoke mode.

The user said: "$ARGUMENTS"

Scope to any named feature or URL. Otherwise find the local dev server and smoke test the core path only.

If the request is really for repro artifacts, use a more formal repro-first workflow instead.
