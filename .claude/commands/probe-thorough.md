# /probe-thorough

Run the browser-probe skill in bug-hunt mode.

The user said: "$ARGUMENTS"

Scope to any named feature or URL. Otherwise find the local dev server and perform a deeper adversarial probe of the whole app.

If the user explicitly wants handoff-grade repro artifacts, use a more formal repro-first workflow instead.
